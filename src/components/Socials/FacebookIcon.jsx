import facebookIcon from '../../assets/facebook.png';
import { Icon } from '../MediaRenderer';

export const FacebookIcon = (props) => {
    return <Icon src={facebookIcon} altText="FaceBook" {...props} />;
};
