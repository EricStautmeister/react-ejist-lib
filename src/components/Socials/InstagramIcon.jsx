import instagramIcon from '../../assets/instagram.png';
import { Icon } from '../MediaRenderer';

export const InstagramIcon = (props) => {
    return <Icon src={instagramIcon} altText="InstagramIcon" {...props}/>;
};
