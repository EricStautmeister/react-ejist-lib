import githubIcon from '../../assets/github.png';
import { Icon } from '../MediaRenderer';

export const GithubIcon = (props) => {
    return <Icon src={githubIcon} altText="GithubIcon" {...props} />;
};
