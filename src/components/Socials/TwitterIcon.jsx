import twitterIcon from '../../assets/twitter.png';
import { Icon } from '../MediaRenderer';

export const TwitterIcon = (props) => {
    return <Icon src={twitterIcon} altText="Twitter" {...props} />;
};
